import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#131723',
      dark: 'black',
    },
    secondary: {
      main: '#1E222F',
    },
  },
})

export default theme
